/**
 * src/routes/attendanceRoutes.js
 * Attendance and check-in endpoints are mapped directly under /api/meetups/:id/checkin in meetupRoutes.js.
 * This file is retained to avoid breaking app scaffolding imports.
 */

const express = require('express');
const router  = express.Router();

module.exports = router;
