/**
 * src/routes/analyticsRoutes.js
 * Meetup analytics endpoints are mapped directly under /api/meetups/:id/analytics in meetupRoutes.js.
 * This file is retained to avoid breaking app scaffolding imports.
 */

const express = require('express');
const router  = express.Router();

module.exports = router;
