/**
 * src/routes/registrationRoutes.js
 * Registration endpoints are mapped directly under /api/meetups/:id/register in meetupRoutes.js.
 * This file is retained to avoid breaking app scaffolding imports.
 */

const express = require('express');
const router  = express.Router();

module.exports = router;
